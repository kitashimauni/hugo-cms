# 流れ
## クライアント側
### 初期化
1. `main.js` が `init()` を呼び出す
2. `fetchConfig()` でHugoのリポジトリ内のconfig (`static/admin/config.yaml`) を取得
3. `fetchFiles()` で全記事を取得、表示

### 記事選択
1. `loadfile()` が呼び出される
2. 

